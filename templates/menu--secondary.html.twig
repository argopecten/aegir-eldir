{#
/**
 * @file
 * Secondary menu template for Eldir theme.
 *
 * Uses BEM naming following Drupal 11 theming standards.
 */
#}
{% macro menu_links(items, attributes, menu_level) %}
  {% if items %}
    {% if menu_level == 0 %}
      <ul{{ attributes.addClass('secondary-nav__menu').setAttribute('id', 'secondary-menu') }}>
    {% else %}
      <ul class="secondary-nav__submenu">
    {% endif %}
    {% for item in items %}
      {% set item_classes = [
        'secondary-nav__item',
        item.is_expanded ? 'secondary-nav__item--expanded',
        item.is_collapsed ? 'secondary-nav__item--collapsed',
        item.in_active_trail ? 'secondary-nav__item--active-trail',
      ] %}
      <li{{ item.attributes.addClass(item_classes) }}>
        {{ link(item.title, item.url, {'class': ['secondary-nav__link']}) }}
        {% if item.below %}
          {{ _self.menu_links(item.below, create_attribute(), menu_level + 1) }}
        {% endif %}
      </li>
    {% endfor %}
    </ul>
  {% endif %}
{% endmacro %}

{{ _self.menu_links(items, attributes, 0) }}
